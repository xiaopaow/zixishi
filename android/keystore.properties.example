# Copy this file to keystore.properties only when restoring an existing key.
# Prefer `pnpm android:signing` for a first-time local setup.
storeFile=C:/secure/location/qishi-beta-release.p12
storePassword=replace-with-your-secret
keyAlias=qishi-beta
keyPassword=replace-with-your-secret
